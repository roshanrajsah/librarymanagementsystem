async function loadBook() { try { const id = queryId(); if (!id) throw new Error('Book not selected.'); const book = await api.get(`/books/${id}`); document.getElementById('bookDetails').innerHTML = `<div class="card"><div class="page-heading"><div><h1>${book.title}</h1><p>${book.author_name} · ${book.genre_name}</p></div><a class="button secondary" href="/book-add-edit?id=${book.id}">Edit book</a></div><div class="grid"><div>${book.cover_image ? `<img style="max-width:220px;border-radius:12px" src="/uploads/${book.cover_image}" alt="${book.title}">` : '<div class="empty">No cover uploaded</div>'}</div><div class="card"><small>AVAILABLE STOCK</small><strong style="font-size:32px">${book.stock}</strong><hr><p><strong>Author</strong><br>${book.author_name}</p><p><strong>Genre</strong><br>${book.genre_name}</p><p><strong>Description</strong><br>${book.description || '—'}</p></div></div></div>`; } catch (error) { document.getElementById('bookDetails').innerHTML = `<div class="empty">${error.message}</div>`; } }
loadBook();
