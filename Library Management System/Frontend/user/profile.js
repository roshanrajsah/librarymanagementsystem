const fields = Object.fromEntries(['firstName','lastName','gender','phoneNumber','address'].map(key => [key, document.getElementById(key)]));
async function loadProfile() { const res = await fetch(`/api/users/${user.id}`); const data = await res.json(); Object.entries(fields).forEach(([key,el]) => el.value=data[key]||''); document.getElementById('profileName').textContent=`${data.firstName} ${data.lastName}`; document.getElementById('profileMeta').textContent=`Library member · @${data.username}`; }
document.getElementById('profileForm').addEventListener('submit', async e => { e.preventDefault(); try { const res=await fetch(`/api/users/${user.id}`,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(Object.entries(fields).map(([key,el])=>[key,el.value.trim()])))}); const data=await res.json(); if(!res.ok) throw new Error(data.error); localStorage.setItem('user',JSON.stringify(data)); toast('Profile updated.'); loadProfile(); } catch(error){toast(error.message,'error')} }); loadProfile();
